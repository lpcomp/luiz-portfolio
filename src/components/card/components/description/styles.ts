import styled from 'styled-components';

export const Content = styled.div`    
    display: flex;
    align-items: center;
    justify-content: space-between;

    .descTxt {
        padding: 0 3% 0 0;
    }
    
    .btPlus {
        font-size: 25px;
        color: #04776d;
        cursor: pointer;
    }
`;